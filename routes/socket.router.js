// const loginController = require('../controllers/login.controller');
// const chatController = require('../controllers/chat.controller');

// module.exports = (io) => {
//   loginController(io.of('/login'));
//   chatController(io.of('/chat'));
// };

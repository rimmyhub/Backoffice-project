<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
      crossorigin="anonymous"
    ></script>
    <script src="../views/index.ejs" defer></script>
    <script src="../views/static/js/mypage.js" defer></script>
    <title>배달의 팔족</title>
  </head>
  <body>
    <!-- header -->
    <%- include('includes/header') %>
    <!-- main -->
    <main class="my-page">
      <!-- 유저 템플릿 -->
      <section class="profile-container">
        <div class="profile-wrap">
          <div class="text-wrap">
            <!-- user : 사장 데이터 -->
            <h2><%= user.name %>사장님 반갑습니다!</h2>
            <p><span class="content-title">포인트</span><%= user.point %></p>
            <p><span class="content-title">상호명</span><%= user.owner_name %></p>
            <p><span class="content-title">사업자 등록번호</span><%= user.owner_number %></p>
          </div>
          <button>정보 수정</button>
        </div>
        <div class="profile-content-wrap">
          <h3>나의 가게</h3>
          <div class="text-wrap">
            <!-- data : 레스토랑 데이터임 (이름바꾸??) -->
            <p><span class="content-title">가게명</span><%= data.name %></p>
            <p><span class="content-title">주소</span><%= data.address %></p>
            <p><span class="content-title">전화번호</span><%= data.phone_num %></p>
            <p><span class="content-title">영업시간</span><%= data.biz_hours %></p>
          </div>
          <button>가게정보 수정</button>
        </div>
      </section>
      <hr />
      <section class="menu-container">
        <!-- get : 메뉴 조회 데이터임 (이름바꾸??) -->
        <h2>메뉴</h2>
        <ul>
          <% get.forEach((menu)=>{ %>
          <li>
            <div class="img-wrap">
              <img src="menu_image" alt="" />
            </div>
            <div class="text-wrap">
              <p><%= menu.name %></p>
              <p><%= menu.price %></p>
            </div>
          </li>
          <% }) %>
        </ul>
        <% if (get.length) { %>
        <button>메뉴 수정</button>
        <% } %> <% if (!get.length) { %>
        <p>등록된 메뉴가 없습니다.</p>
        <% } %>
      </section>
      <hr />
      <section class="order-container">
        <h2>주문 내역</h2>
        <ul class="order-list">
          <!-- data : 주문 데이터임 (이름 바꾸야할듯) -->
          <% data.forEach((order) => { %>
          <li class="order-item">
            <div class="text-wrap">
              <p><span class="content-title">주문번호</span><%= order.order_id %></p>
              <p><span class="content-title">음식점명</span><%= order.restaurant_name %></p>
              <p>
                <span class="content-title">주문메뉴</span>
                <%= order.menuDetails[0].menu_name %> <% if (order.menuDetails.length > 1) { %> 외
                <%= order.menuDetails.length - 1 %>개 <% } %>
                <button type="button" class="btn-more">더보기></button>
              </p>
              <p><span class="content-title">주문시간</span><%= order.order_time %></p>
              <p>
                <span class="content-title">주문시간</span><%= order.order_status %><% if
                (order.order_status === '접수완료') { %> 30분 소요됨 <% } %>
              </p>
            </div>
            <% if (order.order_status === '배달완료') { %>
            <button>리뷰쓰기</button>
            <% } %>
          </li>
          <% }) %>
        </ul>
      </section>
    </main>
    <!-- footer -->
    <%- include('includes/footer') %>
    <script></script>
  </body>
</html>
